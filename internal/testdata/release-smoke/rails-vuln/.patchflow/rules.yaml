schema_version: "1.0"
frameworks:
  auto_detect: true
  enabled: [rails]
  disabled: []

framework_extensions:
  rails:
    safe_patterns:
      - pattern: "\\.where\\(.*:\\s"
        reason: "Hash-syntax parameterized query"

rules: []
